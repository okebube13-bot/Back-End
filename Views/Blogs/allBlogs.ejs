<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blog Posts | MyBlog</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

        body {
            font-family: 'Inter', sans-serif;
        }

        .blog-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .blog-card:hover {
            transform: translateY(-5px);
        }

        .line-clamp-2 {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .line-clamp-3 {
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .gradient-text {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
    </style>
</head>

<body class="bg-gray-50 min-h-screen">
    <!-- Header -->
    <header class="sticky top-0 z-10 bg-white shadow-sm border-b border-gray-100">
        <div class="container mx-auto px-4 sm:px-6">
            <div class="flex justify-between items-center py-4">
                <!-- Logo -->
                <div class="flex items-center space-x-2">
                    <a href="/"
                        class="flex items-center space-x-2 text-2xl font-bold text-gray-800 hover:text-indigo-600 transition-colors">
                        <i class="fas fa-pen-nib text-indigo-600"></i>
                        <span>MyBlog</span>
                    </a>
                </div>

                <!-- Navigation -->
                <nav class="hidden md:flex items-center space-x-8">
                    <a href="/" class="text-gray-700 hover:text-indigo-600 transition-colors font-medium">Home</a>
                    <a href="/posts" class="text-indigo-600 font-medium border-b-2 border-indigo-600 pb-1">All Posts</a>
                    <% if (currentUser) { %>
                        <a href="/posts/new"
                            class="text-gray-700 hover:text-indigo-600 transition-colors font-medium">New Post</a>
                        <a href="/dashboard"
                            class="text-gray-700 hover:text-indigo-600 transition-colors font-medium">Dashboard</a>
                        <% } %>
                            <a href="/about"
                                class="text-gray-700 hover:text-indigo-600 transition-colors font-medium">About</a>
                </nav>

                <!-- Auth Section -->
                <div class="flex items-center space-x-4">
                    <% if (currentUser) { %>
                        <div class="hidden sm:flex items-center space-x-2 bg-indigo-50 px-3 py-1.5 rounded-full">
                            <div class="w-8 h-8 rounded-full bg-indigo-100 flex items-center justify-center">
                                <i class="fas fa-user text-indigo-600 text-sm"></i>
                            </div>
                            <span class="text-gray-700 font-medium text-sm">
                                <%= currentUser.username %>
                            </span>
                        </div>
                        <form action="/search" method="post">
                            <input type="text" name="query" placeholder="Search blogs..." />
                            <button type="submit">Search</button>
                        </form>

                        <% } else { %>
                            <a href="/login"
                                class="text-gray-700 font-medium hover:text-indigo-600 transition-colors flex items-center gap-1">
                                <i class="fas fa-sign-in-alt"></i>
                                <span class="hidden sm:inline">Login</span>
                            </a>
                            <a href="/signup"
                                class="bg-indigo-600 text-white font-medium px-4 py-2 rounded-lg hover:bg-indigo-700 transition-colors flex items-center gap-1">
                                <i class="fas fa-user-plus"></i>
                                <span>Sign Up</span>
                            </a>
                            <% } %>

                                <!-- Mobile menu button -->
                                <button class="md:hidden text-gray-700 focus:outline-none">
                                    <i class="fas fa-bars text-xl"></i>
                                </button>
                </div>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 sm:px-6 py-8">
        <!-- Page Header -->
        <div class="text-center mb-12">
            <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">All Blog Posts</h1>
            <p class="text-gray-600 max-w-2xl mx-auto text-lg">Explore our collection of insightful articles, tutorials,
                and stories from our community of writers.</p>

            <% if (currentUser) { %>
                <div class="mt-8">
                    <a href="/posts/new"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-semibold rounded-lg hover:from-indigo-700 hover:to-purple-700 transition-all duration-300 shadow-md hover:shadow-lg">
                        <i class="fas fa-edit"></i>
                        Write a New Blog
                    </a>
                </div>
                <% } %>
        </div>

        <!-- Blog Posts Grid -->
        <% if (blogs.length> 0) { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% blogs.forEach(blog=> { %>
                    <div
                        class="blog-card bg-white rounded-xl shadow-md hover:shadow-xl border border-gray-100 overflow-hidden flex flex-col h-full">
                        <!-- Blog Image -->
                        <% if (blog.imageUrl) { %>
                            <div class="h-48 overflow-hidden">
                                <img src="<%= blog.imageUrl %>" alt="<%= blog.title %>"
                                    class="w-full h-full object-cover transition-transform duration-500 hover:scale-105">
                            </div>
                            <% } else { %>
                                <div
                                    class="h-48 bg-gradient-to-br from-indigo-100 to-purple-100 flex items-center justify-center">
                                    <i class="fas fa-file-alt text-indigo-400 text-5xl"></i>
                                </div>
                                <% } %>

                                    <!-- Blog Content -->
                                    <div class="p-6 flex flex-col flex-grow">
                                        <h2
                                            class="text-xl font-bold text-gray-900 mb-3 line-clamp-2 hover:text-indigo-600 transition-colors">
                                            <%= blog.title %>
                                        </h2>

                                        <p class="text-gray-600 mb-4 line-clamp-3 flex-grow">
                                            <%= blog.body %>
                                        </p>

                                        <!-- Author & Date -->
                                        <div class="mt-auto">
                                            <div class="flex items-center justify-between text-sm text-gray-500 mb-4">
                                                <div class="flex items-center gap-2">
                                                    <div
                                                        class="w-6 h-6 rounded-full bg-indigo-100 flex items-center justify-center">
                                                        <i class="fas fa-user text-indigo-600 text-xs"></i>
                                                    </div>
                                                    <span class="font-medium">
                                                        <%= blog.Author %>
                                                    </span>
                                                </div>
                                                <div class="flex items-center gap-1">
                                                    <i class="far fa-clock"></i>
                                                    <span>
                                                        <%= blog.createdAt ? blog.createdAt.toLocaleDateString() : "N/A"
                                                            %>
                                                    </span>
                                                </div>
                                            </div>

                                            <!-- Action Buttons (for authenticated users) -->
                                            <% if(currentUser){ %>
                                                <div class="flex items-center gap-3 pt-4 border-t border-gray-100">

                                                    <form action="/view" method="POST">
                                                        <input type="hidden" name="id" value="<%= blog._id %>">
                                                        <button type="submit"
                                                            class="inline-flex items-center gap-1 px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors duration-200 text-sm font-medium">
                                                            <i class="fas fa-eye"></i> View
                                                        </button>
                                                    </form>



                                                    </form>
                                                    <a href="/posts/<%= blog._id %>/edit"
                                                        class="inline-flex items-center gap-1 px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition-colors duration-200 text-sm font-medium">
                                                        <i class="fas fa-edit"></i>
                                                        Edit
                                                    </a>
                                                    <form action="/posts/<%= blog._id %>?_method=DELETE" method="POST"
                                                        class="inline">
                                                        <button type="submit"
                                                            class="inline-flex items-center gap-1 px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition-colors duration-200 text-sm font-medium"
                                                            onclick="return confirm('Are you sure you want to delete this post?')">
                                                            <i class="fas fa-trash-alt"></i>
                                                            Delete
                                                        </button>
                                                    </form>
                                                </div>
                                                <% } else { %>
                                                    <a href="/posts/<%= blog._id %>"
                                                        class="w-full inline-flex items-center justify-center gap-1 px-4 py-2 bg-indigo-500 text-white rounded-lg hover:bg-indigo-600 transition-colors duration-200 text-sm font-medium mt-2">
                                                        <i class="fas fa-book-open"></i>
                                                        Read More
                                                    </a>
                                                    <% } %>
                                        </div>
                                    </div>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <!-- Empty State -->
                <div class="text-center py-16 max-w-2xl mx-auto">
                    <div
                        class="w-32 h-32 mx-auto mb-6 rounded-full bg-gradient-to-br from-indigo-100 to-purple-100 flex items-center justify-center">
                        <i class="fas fa-file-alt text-indigo-400 text-4xl"></i>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-800 mb-4">No blog posts yet</h3>
                    <p class="text-gray-600 mb-8">Be the first to share your thoughts and ideas with our community.</p>
                    <% if (currentUser) { %>
                        <a href="/posts/new"
                            class="inline-flex items-center gap-2 px-8 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-semibold rounded-lg hover:from-indigo-700 hover:to-purple-700 transition-all duration-300 shadow-md hover:shadow-lg">
                            <i class="fas fa-plus"></i>
                            Create Your First Post
                        </a>
                        <% } else { %>
                            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                                <a href="/signup"
                                    class="inline-flex items-center gap-2 px-8 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-semibold rounded-lg hover:from-indigo-700 hover:to-purple-700 transition-all duration-300">
                                    <i class="fas fa-user-plus"></i>
                                    Sign Up to Write
                                </a>
                                <a href="/login"
                                    class="inline-flex items-center gap-2 px-8 py-3 border border-indigo-600 text-indigo-600 font-semibold rounded-lg hover:bg-indigo-50 transition-all duration-300">
                                    <i class="fas fa-sign-in-alt"></i>
                                    Login
                                </a>
                            </div>
                            <% } %>
                </div>
                <% } %>

                    <div class="flex justify-center gap-2 mt-8">
                        <% if (currentPage> 1) { %>
                            <a href="/posts?page=<%= currentPage - 1 %>"
                                class="px-3 py-1 border rounded bg-gray-100 hover:bg-gray-200">Prev</a>
                            <% } %>

                                <% for (let i=1; i <=totalPages; i++) { %>
                                    <a href="/posts?page=<%= i %>"
                                        class="px-3 py-1 border rounded <%= i === currentPage ? 'bg-blue-500 text-white' : 'bg-gray-100 hover:bg-gray-200' %>">
                                        <%= i %>
                                    </a>
                                    <% } %>

                                        <% if (currentPage < totalPages) { %>
                                            <a href="/posts?page=<%= currentPage + 1 %>"
                                                class="px-3 py-1 border rounded bg-gray-100 hover:bg-gray-200">Next</a>
                                            <% } %>
                    </div>

    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-16">
        <div class="container mx-auto px-4 sm:px-6 py-8">
            <div class="flex flex-col md:flex-row justify-between items-center">
                <div class="flex items-center space-x-2 mb-4 md:mb-0">
                    <i class="fas fa-pen-nib text-indigo-600 text-xl"></i>
                    <span class="text-xl font-bold text-gray-800">MyBlog</span>
                </div>
                <div class="flex space-x-6">
                    <a href="/about" class="text-gray-600 hover:text-indigo-600 transition-colors">About</a>
                    <a href="#" class="text-gray-600 hover:text-indigo-600 transition-colors">Privacy</a>
                    <a href="#" class="text-gray-600 hover:text-indigo-600 transition-colors">Terms</a>
                    <a href="#" class="text-gray-600 hover:text-indigo-600 transition-colors">Contact</a>
                </div>
            </div>
            <div class="border-t border-gray-200 mt-6 pt-6 text-center text-gray-500 text-sm">
                <p>© 2023 MyBlog. All rights reserved.</p>
            </div>
        </div>
    </footer>
</body>

</html>