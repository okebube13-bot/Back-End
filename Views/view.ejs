<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= blog.title %> | My Blog
    </title>

    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50 text-gray-800">

    <!-- Container -->
    <div class="max-w-4xl mx-auto px-4 py-10">

        <!-- Blog Card -->
        <article class="bg-white rounded-2xl shadow-sm p-6 md:p-10 border border-gray-100">
            <h1 class="text-3xl md:text-4xl font-extrabold text-gray-900 mb-6 leading-tight">
                <%= blog.title %>
            </h1>

            <% if (blog.imageUrl) { %>
                <div class="mb-6">
                    <img src="<%= blog.imageUrl %>" alt="Blog image"
                        class="w-full h-72 object-cover rounded-xl shadow-sm border border-gray-200">
                </div>
                <% } %>

                    <div class="prose prose-lg max-w-none text-gray-700 leading-relaxed mb-8">
                        <%= blog.body %>
                    </div>

                    <div class="flex justify-between items-center text-sm text-gray-500 border-t pt-4">
                        <span>✍️ By <span class="font-medium text-gray-700">
                                <%= blog.Author %>
                            </span></span>
                        <a href="/posts" class="text-blue-600 hover:underline font-medium">← Back to Blogs</a>
                    </div>
        </article>
    </div>

</body>

</html>