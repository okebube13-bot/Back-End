<!DOCTYPE html>
<html>

<head>
    <title>All Blogs</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50 min-h-screen py-8">
    <div class="max-w-4xl mx-auto px-4">
        <h1 class="text-4xl font-bold text-gray-800 mb-8 text-center">All Blogs</h1>
        <a href="/posts/new"
            class="inline-flex items-center gap-2 px-6 py-3 bg-green-500 text-white rounded-lg hover:bg-green-600 transition-colors duration-200 font-medium">
            ✍️ Write a new blog
        </a>

        <% if (blogs.length> 0) { %>
            <div class="grid gap-6 md:grid-cols-2">
                <% blogs.forEach(blog=> { %>
                    <div
                        class="blog-card bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300 p-6 border border-gray-200">
                        <h2 class="text-xl font-semibold text-gray-800 mb-3 line-clamp-2">
                            <%= blog.title %>
                        </h2>
                        <p class="text-gray-600 mb-4 line-clamp-3">
                            <%= blog.body %>
                        </p>
                        <small class="text-gray-500 text-sm block mb-4">
                            By <span class="font-medium">
                                <%= blog.author %>
                            </span> |
                            Created: <span class="font-medium">
                                <%= blog.createdAt ? blog.createdAt.toLocaleString() : "N/A" %>
                            </span>
                        </small>
                        <div class="flex items-center gap-3 pt-4 border-t border-gray-100">
                            <a href="/posts/<%= blog._id %>/edit"
                                class="inline-flex items-center gap-1 px-3 py-2 bg-blue-500 text-white rounded-md hover:bg-blue-600 transition-colors duration-200 text-sm">
                                ✏️ Edit
                            </a>
                            <form action="/posts/<%= blog._id %>?_method=DELETE" method="POST" class="inline">
                                <button type="submit"
                                    class="inline-flex items-center gap-1 px-3 py-2 bg-red-500 text-white rounded-md hover:bg-red-600 transition-colors duration-200 text-sm">
                                    🗑️ Delete
                                </button>
                            </form>
                        </div>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <div class="text-center py-12">
                    <p class="text-gray-600 text-lg mb-4">No blogs yet.</p>
                    <a href="/new"
                        class="inline-flex items-center gap-2 px-6 py-3 bg-green-500 text-white rounded-lg hover:bg-green-600 transition-colors duration-200 font-medium">
                        ✍️ Write one?
                    </a>
                </div>
                <% } %>
    </div>
</body>

</html>